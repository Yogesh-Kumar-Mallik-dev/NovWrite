#!/usr/bin/env bash
# ==============================================================================
# NovWrite QR Code Alias (./qr -> ./run.sh qr)
# Platform Support: Linux, macOS (Darwin), Windows (Git Bash / MSYS2 / WSL)
# Usage: ./qr [options]
# ==============================================================================

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT_DIR/run.sh" qr "$@"
