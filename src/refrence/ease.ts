export const SPRING_PRESS = {
  type: "spring",
  stiffness: 500,
  damping: 35,
} as const;

/** Delay before triggering the View Transition so the switch thumb begins animating. */
export const THEME_SWITCH_DELAY = 200;
